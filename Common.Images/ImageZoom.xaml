<!-- ReSharper disable once UnusedType.Global -->
<UserControl
    x:Class="Common.Images.ImageZoom"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:imag="clr-namespace:Imaging.Gifs;assembly=Imaging"
    mc:Ignorable="d"
    Name="ImgZoom"
    d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!--
            No hardcoded color here on purpose - ImageZoom is a reusable control, and forcing a
            color scheme onto it would be a bad call for anyone embedding it elsewhere. ScrollViewer
            defaults to an opaque SystemColors.Window (white) background when nothing is set, which
            is why this used to look "aggressively white" regardless of the host window's own theme.
            Binding to the UserControl's own (inherited, already-existing) Background property means
            it's transparent by default, and each consumer just sets Background="..." on the
            <ImageZoom> element itself to opt into whatever look fits.
        -->
        <ScrollViewer x:Name="ScrollView"
                      Background="{Binding Background, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Auto">
            <Canvas x:Name="MainCanvas"
                    Background="Transparent"
                    ClipToBounds="False"
                    MouseDown="Canvas_MouseDown"
                    MouseUp="Canvas_MouseUp"
                    MouseMove="Canvas_MouseMove"
                    MouseWheel="Canvas_MouseWheel"
                    MouseRightButtonUp="Canvas_MouseRightButtonUp">

                <imag:ImageGif x:Name="BtmImage"
                               Stretch="None"
                               SnapsToDevicePixels="True"
                               UseLayoutRounding="True"
                               RenderOptions.BitmapScalingMode="NearestNeighbor"
                               ImageLoaded="BtmImage_ImageLoaded"
                               AutoStart="{Binding ElementName=ImgZoom, Path=AutoplayGifImage}">

                    <imag:ImageGif.RenderTransform>
                        <MatrixTransform x:Name="MainMatrixTransform" />
                    </imag:ImageGif.RenderTransform>
                </imag:ImageGif>
            </Canvas>
        </ScrollViewer>
    </Grid>
</UserControl>