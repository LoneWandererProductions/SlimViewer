<UserControl x:Class="Common.Images.Thumbnails"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800" Loaded="UserControl_Loaded">
    <!--
        Same reasoning as ImageZoom: no hardcoded color, this is a reusable control. ScrollViewer's
        default opaque white background otherwise ignores whatever the host window's theme is.
        Binding to the UserControl's own inherited Background keeps it transparent unless a
        consumer explicitly sets Background="..." on the <Thumbnails> element.
    -->
    <ScrollViewer x:Name="MainScrollViewer"
                  Background="{Binding Background, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto">
        <ScrollViewer.LayoutTransform>
            <ScaleTransform ScaleY="-1" />
        </ScrollViewer.LayoutTransform>

        <Grid x:Name="Thb">
            <Grid.LayoutTransform>
                <ScaleTransform ScaleY="-1" />
            </Grid.LayoutTransform>

        </Grid>
    </ScrollViewer>
</UserControl>