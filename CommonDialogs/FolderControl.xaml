<UserControl x:Class="CommonDialogs.FolderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <!-- Row/Column Definitions -->
        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <!-- Display Path -->
            <RowDefinition Height="5" />
            <RowDefinition Height="25" />
            <!-- Path Input -->
            <RowDefinition Height="5" />
            <RowDefinition Height="25" />
            <!-- Navigation Buttons -->
            <RowDefinition Height="5" />
            <RowDefinition Height="25" />
            <!-- Shortcut 1 -->
            <RowDefinition Height="5" />
            <RowDefinition Height="25" />
            <!-- Shortcut 2 -->
            <RowDefinition Height="5" />
            <RowDefinition Height="25" />
            <!-- Shortcut 3 -->
            <RowDefinition Height="5" />
            <RowDefinition Height="25" />
            <!-- Shortcut 4 -->
            <RowDefinition Height="5" />
            <RowDefinition Height="25" />
            <!-- Shortcut 5 -->
            <RowDefinition Height="5*" />
            <!-- Spacer -->
            <RowDefinition />
            <!-- TreeView -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60" />
            <!-- Buttons Column -->
            <ColumnDefinition Width="5" />
            <ColumnDefinition />
            <!-- TreeView Column -->
            <ColumnDefinition Width="50" />
            <!-- Nav Buttons -->
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="50" />
            <!-- Explorer Button -->
            <ColumnDefinition Width="5" />
        </Grid.ColumnDefinitions>

        <!-- Display Path -->
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"
                   Text="{Binding Paths}" TextWrapping="Wrap"
                   VerticalAlignment="Center" TextTrimming="CharacterEllipsis"
                   Background="White" />

        <!-- Path Input -->
        <TextBox Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3"
                 Text="{Binding LookUp}" TextWrapping="Wrap" VerticalAlignment="Center" />

        <!-- Navigation Buttons -->
        <Button Content="Up" Grid.Column="3" Command="{Binding UpCommand}" />
        <Button Content="Go" Grid.Row="2" Grid.Column="3" Command="{Binding GoCommand}" />
        <Button Content="Exp" Grid.Row="2" Grid.Column="5" Command="{Binding ExplorerCommand}" />

        <!-- Shortcut Buttons -->
        <Button Content="Desktop" Grid.Row="6" Grid.Column="0" Command="{Binding DesktopCommand}" />
        <Button Content="Root" Grid.Row="4" Grid.Column="0" Command="{Binding RootCommand}" />
        <Button Content="Docs" Grid.Row="8" Grid.Column="0" Command="{Binding DocsCommand}" />
        <Button Content="Personal" Grid.Row="10" Grid.Column="0" Command="{Binding PersonalCommand}" />
        <Button Content="Pictures" Grid.Row="12" Grid.Column="0" Command="{Binding PicturesCommand}" />
        <Button Content="Folder" Grid.Row="14" Grid.Column="0" Command="{Binding CreateFolderCommand}" />

        <!-- TreeView -->
        <TreeView ItemsSource="{Binding FolderItems}" Grid.Row="4" Grid.Column="2" Grid.RowSpan="12"
                  Grid.ColumnSpan="4">
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                </Style>
            </TreeView.ItemContainerStyle>
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal">
                        <Rectangle Width="16" Height="16" Fill="LightGray" Margin="0,0,5,0" />
                        <TextBlock Text="{Binding Header}" />
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/CommonDialogs;component/Themes/Generic.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
</UserControl>