<UserControl x:Class="SlimControls.DrawingToolBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:images="clr-namespace:Common.Images;assembly=Common.Images"
             xmlns:converters="clr-namespace:Common.Converter;assembly=Common.Converter"
             xmlns:local="clr-namespace:SlimControls"
             x:Name="RootToolbar">

    <UserControl.Resources>
        <converters:EnumToBooleanConverter x:Key="EnumToBoolConverter" />
        <converters:BooleanToVisibilityConverter x:Key="BoolToVis" />

        <!-- THEME-COMPATIBLE RADIOBUTTON STYLE -->
        <Style x:Key="ToolRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="6,3" />
            <Setter Property="Margin" Value="1,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>

                        <ControlTemplate.Triggers>
                            <!-- Hover -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background"
                                        Value="{StaticResource HoverBrush}" />
                                <Setter TargetName="border" Property="BorderBrush"
                                        Value="{StaticResource BorderBrush}" />
                            </Trigger>

                            <!-- Checked -->
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background"
                                        Value="{StaticResource SurfaceBrush}" />
                                <Setter TargetName="border" Property="BorderBrush"
                                        Value="{StaticResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- THEME-COMPATIBLE SEPARATOR -->
        <Style x:Key="VerticalSeparatorStyle" TargetType="Rectangle">
            <Setter Property="Width" Value="1" />
            <Setter Property="Height" Value="20" />
            <Setter Property="Margin" Value="6,2" />
            <Setter Property="Fill" Value="{StaticResource BorderSubtleBrush}" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </UserControl.Resources>

    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <WrapPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="4,2">

            <!-- PRIMARY TOOLS -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <RadioButton Content="✏️ Pencil" GroupName="ToolGroup"
                             Style="{StaticResource ToolRadioButtonStyle}"
                             ToolTip="Draw freehand lines"
                             IsChecked="{Binding Path=ToolState.ActiveTool,
                                         RelativeSource={RelativeSource AncestorType=UserControl},
                                         Mode=TwoWay,
                                         Converter={StaticResource EnumToBoolConverter},
                                         ConverterParameter=Pencil}" />

                <RadioButton Content="🧹 Eraser" GroupName="ToolGroup"
                             Style="{StaticResource ToolRadioButtonStyle}"
                             ToolTip="Erase parts of the image"
                             IsChecked="{Binding Path=ToolState.ActiveTool,
                                         RelativeSource={RelativeSource AncestorType=UserControl},
                                         Mode=TwoWay,
                                         Converter={StaticResource EnumToBoolConverter},
                                         ConverterParameter=Eraser}" />

                <RadioButton Content="🧪 Picker" GroupName="ToolGroup"
                             Style="{StaticResource ToolRadioButtonStyle}"
                             ToolTip="Pick a color from the image"
                             IsChecked="{Binding Path=ToolState.ActiveTool,
                                         RelativeSource={RelativeSource AncestorType=UserControl},
                                         Mode=TwoWay,
                                         Converter={StaticResource EnumToBoolConverter},
                                         ConverterParameter=ColorPicker}" />

                <RadioButton Content="✋ Move" GroupName="ToolGroup"
                             Style="{StaticResource ToolRadioButtonStyle}"
                             ToolTip="Move or pan the canvas"
                             IsChecked="{Binding Path=ToolState.ActiveTool,
                                         RelativeSource={RelativeSource AncestorType=UserControl},
                                         Mode=TwoWay,
                                         Converter={StaticResource EnumToBoolConverter},
                                         ConverterParameter=Move}" />
            </StackPanel>

            <Rectangle Style="{StaticResource VerticalSeparatorStyle}" />

            <!-- BRUSH SETTINGS -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <images:ColorSelection
                    StartColor="{Binding Path=ToolState.BrushColor,
                                 RelativeSource={RelativeSource AncestorType=UserControl},
                                 Mode=TwoWay}"
                    Width="26" Height="22" Margin="2,0,6,0"
                    VerticalAlignment="Center"
                    ToolTip="Select primary brush color" />

                <!-- Brush Size -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                    <TextBlock Text="🖌️" VerticalAlignment="Center"
                               Margin="0,0,2,0" FontSize="11"
                               Foreground="{StaticResource TextPrimaryBrush}" />

                    <Slider Minimum="1" Maximum="50" Width="70"
                            Value="{Binding Path=ToolState.BrushSize,
                                    RelativeSource={RelativeSource AncestorType=UserControl},
                                    Mode=TwoWay}" />

                    <TextBlock
                        Text="{Binding Path=ToolState.BrushSize,
                                              RelativeSource={RelativeSource AncestorType=UserControl},
                                              StringFormat={}{0:F0}px}"
                        VerticalAlignment="Center" FontSize="10" Margin="4,0,0,0"
                        Width="24" Foreground="{StaticResource TextSecondaryBrush}" />
                </StackPanel>

                <!-- Brush Opacity -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="💧" VerticalAlignment="Center"
                               Margin="0,0,2,0" FontSize="11"
                               Foreground="{StaticResource TextPrimaryBrush}" />

                    <Slider Minimum="0.1" Maximum="1.0" TickFrequency="0.1"
                            IsSnapToTickEnabled="True" Width="60"
                            Value="{Binding Path=ToolState.BrushOpacity,
                                    RelativeSource={RelativeSource AncestorType=UserControl},
                                    Mode=TwoWay}" />

                    <TextBlock
                        Text="{Binding Path=ToolState.BrushOpacity,
                                              RelativeSource={RelativeSource AncestorType=UserControl},
                                              StringFormat={}{0:P0}}"
                        VerticalAlignment="Center" FontSize="10" Margin="4,0,0,0"
                        Width="28" Foreground="{StaticResource TextSecondaryBrush}" />
                </StackPanel>
            </StackPanel>

            <Rectangle Style="{StaticResource VerticalSeparatorStyle}" />

            <!-- SHAPES -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <RadioButton Content="▭ Rect" GroupName="ShapeGroup"
                             Style="{StaticResource ToolRadioButtonStyle}"
                             ToolTip="Draw a rectangle"
                             IsChecked="{Binding Path=ToolState.SelectedShape,
                                         RelativeSource={RelativeSource AncestorType=UserControl},
                                         Mode=TwoWay,
                                         Converter={StaticResource EnumToBoolConverter},
                                         ConverterParameter=Rectangle}" />

                <RadioButton Content="⭕ Ellipse" GroupName="ShapeGroup"
                             Style="{StaticResource ToolRadioButtonStyle}"
                             ToolTip="Draw an ellipse"
                             IsChecked="{Binding Path=ToolState.SelectedShape,
                                         RelativeSource={RelativeSource AncestorType=UserControl},
                                         Mode=TwoWay,
                                         Converter={StaticResource EnumToBoolConverter},
                                         ConverterParameter=Ellipse}" />

                <RadioButton Content="〰️ Freeform" GroupName="ShapeGroup"
                             Style="{StaticResource ToolRadioButtonStyle}"
                             ToolTip="Draw freeform"
                             IsChecked="{Binding Path=ToolState.SelectedShape,
                                         RelativeSource={RelativeSource AncestorType=UserControl},
                                         Mode=TwoWay,
                                         Converter={StaticResource EnumToBoolConverter},
                                         ConverterParameter=Freeform}" />

                <RadioButton Content="⬠ Polygon" GroupName="ShapeGroup"
                             Style="{StaticResource ToolRadioButtonStyle}"
                             ToolTip="Draw polygon"
                             IsChecked="{Binding Path=ToolState.SelectedShape,
                                         RelativeSource={RelativeSource AncestorType=UserControl},
                                         Mode=TwoWay,
                                         Converter={StaticResource EnumToBoolConverter},
                                         ConverterParameter=Polygon}" />
            </StackPanel>

            <!-- AREA MODES -->
            <WrapPanel Orientation="Horizontal" VerticalAlignment="Center"
                       IsEnabled="{Binding Path=ToolState.AreAreaModesEnabled,
                                   RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Visibility="{Binding Path=ToolState.AreAreaModesEnabled,
                                   RelativeSource={RelativeSource AncestorType=UserControl},
                                   Converter={StaticResource BoolToVis}}">

                <Rectangle Style="{StaticResource VerticalSeparatorStyle}" />

                <RadioButton Content="🪣 Fill" GroupName="AreaModeGroup"
                             Style="{StaticResource ToolRadioButtonStyle}"
                             ToolTip="Fill shape"
                             IsChecked="{Binding Path=ToolState.ActiveAreaMode,
                                         RelativeSource={RelativeSource AncestorType=UserControl},
                                         Mode=TwoWay,
                                         Converter={StaticResource EnumToBoolConverter},
                                         ConverterParameter=Fill}" />

                <RadioButton Content="🧱 Texture" GroupName="AreaModeGroup"
                             Style="{StaticResource ToolRadioButtonStyle}"
                             ToolTip="Texture fill"
                             IsChecked="{Binding Path=ToolState.ActiveAreaMode,
                                         RelativeSource={RelativeSource AncestorType=UserControl},
                                         Mode=TwoWay,
                                         Converter={StaticResource EnumToBoolConverter},
                                         ConverterParameter=Texture}" />

                <RadioButton Content="🪄 Filter" GroupName="AreaModeGroup"
                             Style="{StaticResource ToolRadioButtonStyle}"
                             ToolTip="Apply filter"
                             IsChecked="{Binding Path=ToolState.ActiveAreaMode,
                                         RelativeSource={RelativeSource AncestorType=UserControl},
                                         Mode=TwoWay,
                                         Converter={StaticResource EnumToBoolConverter},
                                         ConverterParameter=Filter}" />

                <RadioButton Content="✂️ Clear" GroupName="AreaModeGroup"
                             Style="{StaticResource ToolRadioButtonStyle}"
                             ToolTip="Clear area"
                             IsChecked="{Binding Path=ToolState.ActiveAreaMode,
                                         RelativeSource={RelativeSource AncestorType=UserControl},
                                         Mode=TwoWay,
                                         Converter={StaticResource EnumToBoolConverter},
                                         ConverterParameter=Erase}" />

                <Rectangle Style="{StaticResource VerticalSeparatorStyle}" />

                <!-- DYNAMIC OPTIONS -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                    <!-- Fill -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center"
                                Visibility="{Binding Path=ToolState.Fill.Enabled,
                                             RelativeSource={RelativeSource AncestorType=UserControl},
                                             Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="Color:" Margin="2,0,4,0" FontSize="11"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextSecondaryBrush}" />

                        <images:ColorSelection
                            StartColor="{Binding Path=ToolState.Fill.Color,
                                         RelativeSource={RelativeSource AncestorType=UserControl},
                                         Mode=TwoWay}"
                            Width="26" Height="22" />
                    </StackPanel>

                    <!-- Texture -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center"
                                Visibility="{Binding Path=ToolState.Texture.Enabled,
                                             RelativeSource={RelativeSource AncestorType=UserControl},
                                             Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="Pattern:" Margin="2,0,4,0" FontSize="11"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextSecondaryBrush}" />

                        <ComboBox
                            ItemsSource="{Binding Path=ToolState.Textures,
                                                RelativeSource={RelativeSource AncestorType=UserControl}}"
                            SelectedItem="{Binding Path=ToolState.Texture.TextureName,
                                                 RelativeSource={RelativeSource AncestorType=UserControl},
                                                 Mode=TwoWay}"
                            Width="120" FontSize="11"
                            Foreground="{StaticResource TextPrimaryBrush}"
                            Background="{StaticResource InputBrush}"
                            BorderBrush="{StaticResource BorderBrush}" />
                    </StackPanel>

                    <!-- Filter -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center"
                                Visibility="{Binding Path=ToolState.Filter.Enabled,
                                             RelativeSource={RelativeSource AncestorType=UserControl},
                                             Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="Effect:" Margin="2,0,4,0" FontSize="11"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextSecondaryBrush}" />

                        <ComboBox
                            ItemsSource="{Binding Path=ToolState.Filters,
                                                RelativeSource={RelativeSource AncestorType=UserControl}}"
                            SelectedItem="{Binding Path=ToolState.Filter.FilterName,
                                                 RelativeSource={RelativeSource AncestorType=UserControl},
                                                 Mode=TwoWay}"
                            Width="120" FontSize="11"
                            Foreground="{StaticResource TextPrimaryBrush}"
                            Background="{StaticResource InputBrush}"
                            BorderBrush="{StaticResource BorderBrush}" />
                    </StackPanel>
                </StackPanel>
            </WrapPanel>
        </WrapPanel>
    </ScrollViewer>
</UserControl>