<Window x:Class="SlimViews.Rename"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        x:ClassModifier="internal"
        Title="Rename" Height="400" Width="400">

    <Grid IsEnabled="{Binding IsNotWorking}" Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="50*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="15" />
            <RowDefinition Height="*" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Content="Replacement String:" Grid.Column="0" Grid.Row="0" VerticalAlignment="Bottom" />
        <TextBox Grid.Column="0" Grid.Row="1" TextWrapping="Wrap" Height="25"
                 Text="{Binding Path=Replacement, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 ToolTip="Enter the target text you want to add, remove, or find." />

        <Label Content="New String (For Replace):" Grid.Column="2" Grid.Row="0" VerticalAlignment="Bottom" />
        <TextBox Grid.Column="2" Grid.Row="1" TextWrapping="Wrap" Height="25"
                 Text="{Binding Replacer, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 ToolTip="Enter the new text that will replace the old text." />

        <Button Content="Add Appendages" Grid.Column="0" Grid.Row="2" Margin="0,5,0,0" Height="25"
                Command="{Binding AddCommand}" ToolTip="Appends the target text to the filenames." />
        <Button Content="Remove Appendages" Grid.Column="2" Grid.Row="2" Margin="0,5,0,0" Height="25"
                Command="{Binding RemoveAppendageCommand}" ToolTip="Removes the target text from the beginning or end." />

        <Button Content="Remove Content" Grid.Column="0" Grid.Row="3" Margin="0,5,0,0" Height="25"
                Command="{Binding RemoveCommand}" ToolTip="Removes all instances of the target text." />
        <Button Content="Replace String" Grid.Column="2" Grid.Row="3" Margin="0,5,0,0" Height="25"
                Command="{Binding ReplaceCommand}" ToolTip="Replaces the target text with the new string." />

        <Label Content="Chars to Strip from Start:" Grid.Column="0" Grid.Row="4" VerticalAlignment="Bottom"
               Margin="0,5,0,0" />
        <TextBox Grid.Column="0" Grid.Row="5" TextWrapping="Wrap" Height="25"
                 Text="{Binding Numbers, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <Button Content="Remove Appendages at" Grid.Column="2" Grid.Row="5" Margin="0,5,0,0" Height="25"
                Command="{Binding AppendagesAtCommand}" ToolTip="Removes specific number of characters from start." />

        <Button Content="Reorder Numbers" Grid.Column="0" Grid.Row="6" Margin="0,5,0,0" Height="25"
                Command="{Binding ReorderCommand}" ToolTip="Automatically reorders the numbers found." />

        <DataGrid Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="3"
                  ItemsSource="{Binding PreviewItems}"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal" BorderBrush="LightGray">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Original Name" Binding="{Binding OriginalName}" Width="*" />
                <DataGridTextColumn Header="New Name" Binding="{Binding NewName}" Width="*" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80" />
            </DataGrid.Columns>
        </DataGrid>

        <Button Content="Apply Changes to Disk" Grid.Row="10" Grid.Column="2" Height="35"
                FontWeight="Bold" Background="#4CAF50" Foreground="White"
                Command="{Binding ApplyChangesCommand}"
                ToolTip="Permanently rename the files on your hard drive." />
    </Grid>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/SlimViewer;component/Themes/Generic.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
</Window>