<Window x:Class="SlimViews.Tooling.Converter"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        x:ClassModifier="internal"
        Title="Format Converter" 
        Width="340" 
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner" 
        ResizeMode="NoResize"
        Background="#FAFAFA">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/SlimViewer;component/Themes/Generic.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header Title -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="🔄" FontSize="16" VerticalAlignment="Center" Margin="0,0,6,0" />
            <TextBlock Text="Convert Image Formats" FontWeight="Bold" FontSize="14" VerticalAlignment="Center" Foreground="#333" />
        </StackPanel>

        <!-- Source Format Row -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="From:" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#555" />
            <ComboBox Grid.Column="1"
                      Height="28"
                      VerticalContentAlignment="Center"
                      ItemsSource="{Binding Path=SelectedSource}"
                      SelectedItem="{Binding Path=SourceSelect, UpdateSourceTrigger=PropertyChanged}"
                      ToolTip="Select the current format of the images you want to convert" />
            <Label Grid.Column="2"
                   Content="{Binding Path=Source}"
                   VerticalAlignment="Center"
                   Margin="6,0,0,0"
                   FontWeight="Bold"
                   Foreground="#777"
                   ToolTip="Current Source Extension" />
        </Grid>

        <!-- Directional Flow Indicator -->
        <TextBlock Grid.Row="2" Text="⬇️" HorizontalAlignment="Center" Margin="0,6" FontSize="12" Opacity="0.7" />

        <!-- Target Format Row -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="To:" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#555" />
            <ComboBox Grid.Column="1"
                      Height="28"
                      VerticalContentAlignment="Center"
                      ItemsSource="{Binding Path=ExtensionSource}"
                      SelectedItem="{Binding Path=ExtensionSelect, UpdateSourceTrigger=PropertyChanged}"
                      ToolTip="Select the new format you want to convert the images into" />
            <Label Grid.Column="2"
                   Content="{Binding Path=Target}"
                   VerticalAlignment="Center"
                   Margin="6,0,0,0"
                   FontWeight="Bold"
                   Foreground="#777"
                   ToolTip="Current Target Extension" />
        </Grid>

        <!-- Action Button -->
        <Button Grid.Row="5"
                Content="✨ Convert Formats"
                Height="32"
                FontWeight="SemiBold"
                Command="{Binding OkayCommand}"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                ToolTip="Apply conversion settings to target files" />
    </Grid>
</Window>