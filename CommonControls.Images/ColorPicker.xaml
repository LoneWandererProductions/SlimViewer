<UserControl x:Class="CommonControls.Images.ColorPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="450">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" MinHeight="200" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Grid.Column="0">
            <Grid Background="Transparent"
                  MouseDown="OnMouseDown" 
                  MouseMove="OnMouseMove" 
                  MouseUp="OnMouseUp">

                <Image x:Name="PickerImage" Stretch="Uniform"
                       RenderOptions.BitmapScalingMode="HighQuality"
                       IsHitTestVisible="False"/>

                <Canvas x:Name="CursorCanvas" IsHitTestVisible="False">
                    <Ellipse x:Name="HueCursor" Width="14" Height="14"
                             Stroke="Black" StrokeThickness="2" Fill="Transparent"
                             Visibility="Collapsed">
                        <Ellipse.Effect>
                            <DropShadowEffect Color="White" BlurRadius="4" ShadowDepth="0" Opacity="1" />
                        </Ellipse.Effect>
                    </Ellipse>

                    <Ellipse x:Name="SvCursor" Width="10" Height="10"
                             Stroke="White" StrokeThickness="2" Fill="Transparent"
                             Visibility="Collapsed">
                        <Ellipse.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="2" ShadowDepth="0" Opacity="0.8" />
                        </Ellipse.Effect>
                    </Ellipse>
                </Canvas>
            </Grid>
        </Grid>

        <Grid Grid.Row="0" Grid.Column="1" Margin="10,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Slider Grid.Row="0" Orientation="Vertical" Maximum="255" Minimum="0"
                    Value="{Binding Alpha, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    HorizontalAlignment="Center" />
            <TextBlock Grid.Row="1" Text="{Binding Alpha}" HorizontalAlignment="Center" Margin="0,5,0,0" />
        </Grid>

        <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,10,0,0"
              Visibility="{Binding ShowTextBoxes, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="10" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="10" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Hue" Padding="0" />
            <TextBox Grid.Row="1" Grid.Column="0" Text="{Binding Hue, StringFormat=N1, UpdateSourceTrigger=LostFocus}" />

            <Label Grid.Row="0" Grid.Column="2" Content="Sat" Padding="0" />
            <TextBox Grid.Row="1" Grid.Column="2" Text="{Binding Sat, StringFormat=N2, UpdateSourceTrigger=LostFocus}" />

            <Label Grid.Row="0" Grid.Column="4" Content="Val" Padding="0" />
            <TextBox Grid.Row="1" Grid.Column="4" Text="{Binding Val, StringFormat=N2, UpdateSourceTrigger=LostFocus}" />

            <Label Grid.Row="3" Grid.Column="0" Content="R" Padding="0" />
            <TextBox Grid.Row="4" Grid.Column="0" Text="{Binding R, UpdateSourceTrigger=PropertyChanged}" />

            <Label Grid.Row="3" Grid.Column="2" Content="G" Padding="0" />
            <TextBox Grid.Row="4" Grid.Column="2" Text="{Binding G, UpdateSourceTrigger=PropertyChanged}" />

            <Label Grid.Row="3" Grid.Column="4" Content="B" Padding="0" />
            <TextBox Grid.Row="4" Grid.Column="4" Text="{Binding B, UpdateSourceTrigger=PropertyChanged}" />

            <TextBox Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="5"
                     Text="{Binding Hex, UpdateSourceTrigger=LostFocus}" />
        </Grid>
    </Grid>
</UserControl>